{
  "servers": [
    {
      "name": "api-docker-local",
      "host": "unix:///var/run/docker.sock",
      "description": "Docker daemon running on the same machine as the API",
      "default": true
    },
    {
      "name": "api-docker-remote",
      "host": "tcp://192.168.1.100:2375",
      "description": "Docker daemon running on a remote machine",
      "default": false
    }
  ]
}
